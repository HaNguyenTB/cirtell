{
  "name": "cirtell",
  "version": "1.0.0",
  "private": true,
  "description": "Cirtell workspace tooling",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "diagrams:render": "node scripts/render-mermaid.mjs",
    "diagrams:clean": "node scripts/render-mermaid.mjs --clean",
    "diagrams:check": "node scripts/render-mermaid.mjs --check"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0"
  }
}
